import common from './common.json';
import visualBuilder from './visual-builder.json';
import dashboard from './dashboard.json';
import settings from './settings.json';

const messages = {
  common,
  visualBuilder,
  dashboard,
  settings,
};

export default messages;
